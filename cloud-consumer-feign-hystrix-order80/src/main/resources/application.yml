server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka

ribbon:
  #建立连接时间
  ReadTimeout: 5000
  #读取资源时间
  ConnectTimeout: 5000

feign:
  hystrix:
    enabled: true